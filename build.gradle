subprojects { subProject ->

    apply plugin: 'java'
    sourceCompatibility = 1.8

    repositories {
        mavenCentral()
    }

    // Set up automatic dependency for all other modules
    if (subProject.name != 'common') {
        dependencies {
            compile project(':common')
        }
    }
}